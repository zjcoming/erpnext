[project]
name = "process_simplification"
authors = [
    { name = "Custom ERPNext Implementation", email = "admin@example.com" }
]
description = "Simplified manufacturing flow app for ERPNext"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=16.32.0,<17.0.0"
erpnext = ">=16.33.0,<17.0.0"
