[project]
name = "process_simplification"
authors = [
    { name = "重庆恒算科技有限公司", email = "contact@hengsuankeji.com" }
]
description = "恒算 ERP 企业经营管理系统，基于 ERPNext 定制开发"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=16.32.0,<17.0.0"
erpnext = ">=16.33.0,<17.0.0"
