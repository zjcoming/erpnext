# 恒算 ERP 库房采购待办与供应商选择框生产发布

2026-09-22 已完成推送 `rc/develop-v16` 并部署 prod。生产完成时间为北京时间 21:03；本次按推送后的完整代码构建，包含库房待办、供应商选择框和此前已提交的登录页备案号。

## 版本

- 镜像：`hengsuan/erpnext:v16.33.0-hs.20260922.3`。
- 完整源码提交：`26cebd6caaf1430b2b1e3d106cf7a6badd683e61`，包含登录页备案号提交 `63ee0f9fd94e0aabab1732e9b4ec2ca3ba8eeb62`。
- 镜像 ID：`sha256:24a74bf3d327ffa647896758b8fcb539b5cd88aef1b2deca69a0c100fa048de2`。
- 镜像包 SHA-256：`db3b255f056d1477633a2d4d7878a7db2fc37f4ef2ae86913afa6108c4e7255e`，本地和生产服务器一致。
- 构建时工作区无未提交改动，远程 v16 与镜像源码提交一致；镜像内 357 个自定义 App 源文件与提交逐一匹配，Frappe / ERPNext 固定补丁通过指纹检查。
- 升级前镜像：`hengsuan/erpnext:v16.33.0-hs.20260922.2`；生产目录 `/opt/hengsuan-erp`，Gunicorn 保持 3 进程 × 4 线程。

## 生产实际结果

- 使用已有库管李四会话访问库房工作台，显示 **1 张采购申请待跟进**，原六类库存单据待办保持可用。
- 处理按钮进入智连动机电有限公司、范围“待下单 / 确认”的采购申请列表，显示 `MAT-MR-2026-00001`。
- 该申请有 10 项待分配物料。进入详情点击“批量选择供应商”，页面内立即显示 **老刘总供应批发**。
- 当前缺料为 0，原因是已有采购申请覆盖，后续动作已转为采购跟进；没有将通知已读当作业务完成。
- 生产角色只读 API 核对待办与目标列表数量一致，标准供应商搜索返回上述未停用供应商。
- 公网 ping 返回 `200 / pong`，登录页及五项脚本样式均返回 200，静态文件摘要与镜像源码一致；登录页包含备案号 `蜀ICP备2026054262号-1`。
- 六个常驻应用服务均为相同新镜像，前后端健康，两个队列工作进程在线，调度器正常；维护模式为 0、测试开关为 false、开发模式为 0。

## 数据保护与验证

先完成新镜像独立安装，再把生产完整备份恢复到独立站点执行迁移；邮件静默、后台任务暂停。正式切换时进入维护状态，停止写入相关服务，保存最终备份后再迁移。

- 最终备份目录：`/opt/hengsuan-erp/releases/20260922.3/pre-upgrade-backup`。
- 文件前缀：`20260922_210140-erp_hengsuankeji_com`；数据库、公开附件、私有附件、站点配置共 4 件。本地受限副本逐件 SHA-256 校验一致。
- 恢复演练与生产迁移均对比 44 类表的逐字段摘要、密码摘要、角色及数据范围权限和关键系统设置，结果通过，无业务字段差异或新增业务字段。
- 既有迁移钩子会整理关联员工的角色。本轮按升级前快照精确保留原账号角色和角色配置，验证权限及密码摘要一致；用户和角色表修改时间、系统设置修改时间的变化单独记录。
- 岗位首页导航门禁通过，批次功能保持原值：总开关 0、批次物料 0。
- 旧镜像、`previous.env`、`previous-compose.yaml` 和最终备份均保留；备份和账号资料不提交 Git。

## 测试与验收边界

前端 76 项回归通过；正式镜像独立站点再次运行后端 45 项测试全部通过。此前 8 阶段真实单据流转、5 类实际角色权限检查，以及桌面和 390 × 844 视口待办验收详见实施记录。供应商选择框追加验证覆盖点击展开、名称搜索、批量回填及逐项选择；手机视口内容宽度 390，候选完整可见。

本次生产只查看真实业务数据、打开候选，没有生成测试采购单或做库存过账。生产浏览器核对以桌面为准；实体手机与工厂现场的新订单 → 采购申请 → 正式下单 → 收货完整签收仍由实际岗位完成。

## 证据

`outputs/release-20260922-warehouse/` 保存构建、源码指纹、测试、上传校验、恢复演练及生产结果。关键记录：`image-source-verification.json`、`backend-tests.json`、`restore-gate-final.json`、`restored-warehouse-check.json`、`cloud/data-comparison.json`、`cloud/production-warehouse-verification.json`、`production-services-verified.json`、`public-health.json`、`supplier-browser-checks.json`、`production-browser-checks.json`。受限备份位于 `private/`，该目录不提交 Git。

回滚前应再次停写并保存故障现场，先判断旧代码与当前数据库的兼容性。若需恢复最终备份，必须处理备份之后产生的业务数据，不能直接覆盖。
