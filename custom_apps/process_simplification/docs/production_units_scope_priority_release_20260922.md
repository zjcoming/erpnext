# 恒算 ERP：单位中文显示、仓库组授权及发料入口生产发布

已于北京时间 2026-09-22 23:20:18 完成推送 `rc/develop-v16` 并部署 prod。

## 发布范围与版本

- 完整源码提交：`86b756bfa00a1ef6c6891d46b8c3102ea41a8df9`。从推送后的固定提交归档构建，构建时工作区干净。
- 镜像：`hengsuan/erpnext:v16.33.0-hs.20260922.4`。
- 镜像 ID：`sha256:f4bf342b3ec12419a674d3170e57502965637ea6a0086b759d58c6a90ff3aa6f`，六个生产应用服务一致。
- 镜像包 SHA-256：`04005f6d0b46193085c4545cf8f58843775428af97de747abb6574dd27fa311e`，本地与生产服务器逐字节校验一致。
- 旧镜像：`hengsuan/erpnext:v16.33.0-hs.20260922.3`。Frappe 16.32.0 / ERPNext 16.33.0 及现有版本固定补丁保持一致，Web 并发保持 3 进程 × 4 线程。

本次包含 v16 分支的全部已提交代码，新增行为如下：

1. 库房工作台六类库存待办及批次明细使用系统单位翻译，`Gram → 克`、`Nos → 个`；数量、原始单位和换算规则不变。提交 `86b756bfa0`。
2. 权限管理支持仓库组授权及后代自动继承，保留原生仅节点授权和现有单仓库授权；不会自动把用户改成整组授权。提交 `1f6297d94d`。
3. 部分物料可发料且存在交期分配影响时，生产负责人可选择原有的优先级调整动作；默认仍按建议量申请，其他单据正式预留继续受保护。提交 `99d8dadf09`。

此前已发布的采购待办、供应商搜索等功能一并保留。仓库组授权和发料入口的实施边界分别见 `warehouse_group_scope_acceptance_20260922.md` 和 `production_priority_issue_flexibility_20260922.md`；其中历史“尚未发布”状态由本记录更新。

## 构建与数据保护

Docker Hub 标签查询曾连接中断；改用上次已验证的 Dockerfile 1.7 编译前端固定摘要后完成构建，未更改 Git 源码或运行时依赖。镜像内 359 个自定义应用源文件与提交匹配，Frappe / ERPNext 补丁文件指纹通过。

先完成新镜像独立安装，再把生产数据库、公开附件、私有附件及配置备份恢复到独立数据库和 Redis，暂停后台队列和调度并静默邮件，执行迁移及相关回归。切换 prod 前进入维护、停止写入相关服务，再保存最终完整备份。

- 最终备份：`/opt/hengsuan-erp/releases/20260922.4/pre-upgrade-backup`，四项产物及其本地受限副本均校验一致。
- 数据文件：`20260922_231857-erp_hengsuankeji_com-database.sql.gz`。
- 隔离恢复和生产迁移均对比 44 类表的逐字段摘要，业务字段、角色及数据范围权限、密码摘要和系统设置一致，无新增业务字段。
- 既有迁移钩子整理员工角色的行为，使用升级前账号快照精确保留；用户/角色及系统设置的修改时间变动单独记录，不属于业务数据变化。
- 旧镜像、最终备份、`previous.env`、`previous-compose.yaml` 保留。回滚前需停写并保护升级后新业务，不能直接覆盖数据库。

## 验证结果及边界

相关前端测试 77 项通过；正式镜像在隔离恢复站点运行后端测试 72 项通过，包括库存待办、采购页面、刷新事件、仓库组权限及仓库树。

生产只读验证通过：

- 库管李四权限下的原生 Page 响应已包含中文单位渲染逻辑，生产翻译为 `Gram=克`、`Nos=个`、`Meter=米`、`Kg=千克`。
- 工作台采购动作与目标列表一致，供应商候选查询可用。
- 公网 ping、登录页及六项脚本样式返回 200，静态文件摘要与发布提交匹配。
- 库管实际浏览器刷新后工作台正常显示，缺料 10 项、涉及 1 张销售订单，六类库存待办入口可用。当前库存待办为空，原截图的完工入库单已不在待办中，因此未声称在生产重现原卡片的 `300 克 / 30 个`。
- 发布完成后的浏览器检查未发现新错误；维护窗口期间的三条 Socket.IO 断连记录发生于切换前后端时。
- 六个应用服务运行同一新镜像，前后端健康，队列工作进程及调度正常。维护模式 0、测试开关 false、开发模式 0，批次配置保持原值。

本次未在生产创建测试单据、调整人员授权或进行库存过账。实际负责人选择仓库组和工厂现场的发料签收仍属于岗位验收。

## 证据

`outputs/release-20260922-units/` 包含固定提交构建日志、源码校验、77 项前端及 72 项后端回归、恢复数据比对、镜像上传校验、生产前后状态和浏览器记录。关键文件：`image-source-verification.json`、`restore-gate-final.json`、`backend-tests.json`、`cloud/data-comparison.json`、`cloud/production-warehouse-verification.json`、`production-services-verified.json`、`public-health.json`、`browser-verification.json`。备份和账号资料置于受限目录，不提交 Git。
