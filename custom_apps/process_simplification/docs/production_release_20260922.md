# 恒算 ERP 生产发布记录（2026-09-22）

2026-09-22 11:16:52（北京时间）完成 prod 镜像切换和服务恢复，随后完成公网、配置、服务及浏览器复核。

## 版本和发布范围

- 分支：`rc/develop-v16`，源码提交 `801e16e9f755c5e6e129d57be689ccfb87aaac05`，已推送到 origin。
- 提交发布前工作区的 40 个文件，涉及初始化检查、仓库改名、受限仓库树导航、权限页请求处理、首次向导侧栏兼容、可选半成品仓及其测试和使用指南。
- 镜像：`hengsuan/erpnext:v16.33.0-hs.20260922.1`，从已提交源码构建。
- 镜像 ID：`sha256:47d6ae219e8e060e04f5c76b78b174a52a4ddb5ea3e740a0ce5d168068d8f7cb`。
- 镜像包 SHA-256：`6e971270b9f31b7159ffb9db7b6daab1179d35fed898b5e2a34cd8d4f88ba194`，本地与生产服务器一致。
- 生产站：<https://erp.hengsuankeji.com>；目录 `/opt/hengsuan-erp`；并发保持 3 个 Gunicorn 进程 × 4 个线程。

## 发布前验证

1. 343 项 JavaScript 测试、196 项后端测试及 5 项补丁测试通过。后端在本次正式镜像的隔离容器运行，覆盖初始化、生产计划、快速开单、半成品库存覆盖和仓库路由、实际库存流程、仓库改名与权限树。测试依赖 `freezegun` 仅安装在隔离容器，不改变发布镜像。
2. 镜像内 354 个应用源码文件与 Git 提交逐项 SHA-256 一致，Frappe 与 ERPNext 固定补丁指纹通过。
3. 独立新站完成安装、迁移及 HTTP 健康检查。随后将当日生产预发布备份恢复到独立数据库，执行新版本迁移和数据、权限、配置对比。新站安装检查不等于逐屏初始化向导验收。
4. 进入维护前验证目标镜像可以读取现有部署密钥文件。

## 数据保护及账号迁移处理

维护窗口先关闭写入入口并停止队列、调度器，再保存数据库、公开附件、私有附件和站点配置。最终备份位于：

`/opt/hengsuan-erp/releases/20260922.1/pre-upgrade-backup/20260922_111534-erp_hengsuankeji_com-*`

四件备份均保留服务器原件及本地受限副本，逐件通过 SHA-256 校验；旧镜像 `v16.33.0-hs.20260917.1`、`previous.env` 和 `previous-compose.yaml` 保留。备份、账号快照及密钥不进入 Git。

隔离恢复的首次比较发现，已有 `after_migrate` 中的岗位模板归一化逻辑会给一个关联在职员工的账号补加 `Employee` 角色、调整岗位模板，并重建部分用户的角色子表。这不是本次新增功能。为保持本次发布前的账号配置，发布脚本在维护期间保存账号角色与岗位模板原始行，迁移后检查变化范围，再恢复原有分配并清理用户缓存；不修改密码。保护步骤先在隔离恢复站验证，再用于生产。

最终生产比较覆盖 38 类表、密码摘要和 4 类 Single 配置：业务字段、账号分配、用户数据权限及配置值一致。仅 `User.modified/modified_by`、`Has Role.modified`、`System Settings.modified` 元数据变化；新增的公司默认半成品仓和工单半成品仓字段为空。

原岗位模板迁移实现没有在本次改写，以后执行相同迁移仍需复核账号分配并沿用此保护步骤。原始失败比较和最终通过记录均保留，未将首次比较写成通过。

## 生产复核

- 6 个常驻应用服务均运行本次镜像，backend/frontend 健康，2 个队列工作进程在线，调度器检查正常。
- `maintenance_mode=0`、`allow_tests=false`、`developer_mode=0`；批次总开关为 0，批次物料数为 0。
- 公网 HTTPS ping、登录页及 5 个脚本/样式返回 200，静态文件与源码摘要一致。
- 使用已有 Administrator 浏览器会话检查：公司库存与生产页显示新的默认半成品仓；系统设置显示开用前检查；经营总览、权限管理、生产计划中心、库房工作台、快速开单及原生桌面均加载成功。验证标签页未记录到浏览器错误。
- 未在生产创建测试业务单据，也未替公司选择半成品仓；本次浏览器检查不包含普通岗位重新登录、手机实机或生产过账。

## 剩余开用范围

生产配置检查仍显示默认在制品仓、默认成品仓未明确设置；原料来源仓目前按本公司匹配为“仓库 - 智”，需要管理员核对。这些是发布前已有配置状态，本次保留原值。可选半成品仓留空，继续沿用原料来源仓。

完成配置后仍应使用真实岗位和实际物料/BOM 验收一笔小订单；本次发布检查不能代替工厂业务与设备现场签收。

详细证据位于 `outputs/release-20260922/`：`commit-manifest.txt`、`js-tests.log`、`backend-tests.json`、`image-source-verification.json`、`image-transfer.json`、`restore-comparison.json`（首次失败）、`restore-gate-final.json`、`cloud/data-comparison.json`、`cloud/completed.json`、`production-services-verified.json`、`production-configuration.json`、`public-health.json` 及受限 `private/` 备份。本次隔离栈 `hs-release-20260922` 已停止，卷与证据保留。

回滚前应停止写入并保存故障现场，先判断旧镜像与迁移后数据库是否兼容；需要恢复备份时不得直接覆盖发布后新增的业务数据。
